Hyperbolic Vector Object (HVO) Format:
global {center}, scale

	path define:
path(fill type (or 'none'), collision type (or 'none'), {path start}, z-index,) {x, y} connector {x, y} connector {x, y} ... {x,y} connector;

connector types: line, -arc(center coords), +arc(center coords)

	fill type define:
fill name: region type, RRGGBBAA;
region types are: inner, outer.

